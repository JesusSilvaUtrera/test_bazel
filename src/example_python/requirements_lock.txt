#  In this case it is called 'requirements_lock' because you need to explciitely add all the dependencies here (no other dependencies will be installed)
exceptiongroup==1.2.2
iniconfig==2.0.0
packaging==24.1
pluggy==1.5.0
pytest==8.3.3
tomli==2.0.2
